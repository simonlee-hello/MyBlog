{
  "name": "web-analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "dev": "wrangler dev --config wrangler.jsonc",
    "start": "wrangler dev --config wrangler.jsonc",
    "initSqlLocal": "npx wrangler d1 execute web_analytics --local --file=./schema.sql --config wrangler.jsonc",
    "initSql": "npx wrangler d1 execute web_analytics --file=./schema.sql --remote --config wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231121.0",
    "@types/node": "^20.10.2",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "hono": "^3.10.3"
  }
}
